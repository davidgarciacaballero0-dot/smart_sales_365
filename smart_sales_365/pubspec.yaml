name: smart_sales_365
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  provider: ^6.0.0
  http: ^1.0.0
  shared_preferences: ^2.0.0
  flutter_rating_bar: ^4.0.1

  # --- AÑADIDO PARA LA PASARELA DE PAGO ---
  flutter_stripe: ^10.1.1
  url_launcher: ^6.2.6
  # --- FIN DE LA MODIFICACIÓN ---

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true