name: smart_sales_365
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  provider: ^6.0.0
  http: ^1.0.0
  shared_preferences: ^2.0.0
  
  # --- AÑADIDO PARA LA FUNCIONALIDAD DE RESEÑAS ---
  flutter_rating_bar: ^4.0.1
  # --- FIN DE LA MODIFICACIÓN ---

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true